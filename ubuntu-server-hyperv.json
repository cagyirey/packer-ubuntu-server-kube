{
  "builders": [
    {
      "boot_command": [
        "{{ user `boot_command_prefix` }}",
        "linux /casper/vmlinuz quiet autoinstall ds=nocloud-net\\;s=http://{{ .HTTPIP }}:{{ .HTTPPort }}/ ---",
        "<enter>initrd /casper/initrd",
        "<enter>boot<enter>"
      ],
      "boot_wait": "1s",
      "communicator": "ssh",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{user `disk_size`}}",
      "enable_secure_boot": false,
      "generation": "{{user `hyperv_generation`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_url": "https://releases.ubuntu.com/20.04/{{ user `template` }}.iso",
      "memory": "{{user `memory`}}",
      "output_directory": "{{ user `build_directory` }}/packer-{{user `template`}}-hyperv",
      "shutdown_command": "echo 'vagrant' | sudo -S shutdown -P now",
      "ssh_port": 22,
      "ssh_timeout": "10000s",
      "ssh_username": "vagrant",
      "ssh_private_key_file": "{{ user `ssh_private_key_file` }}",
      "switch_name": "{{user `hyperv_switch`}}",
      "type": "hyperv-iso",
      "vm_name": "{{ user ``template`` }}",
      "http_content": { 
          "user-data": "{{template_dir}}/http/{{user `container_runtime` }}/user-data",
          "meta-data": "{{template_dir}}/http/meta-data"
      }
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "vagrantfile_template": "templates/hyperv/Vagrantfile.template",
      "output": "{{ user `build_directory` }}/{{user `template`}}.{{.Provider}}.box"
    }
  ],
  "variables": {
    "template": "ubuntu-20.04.2-live-server-amd64",
    "version": "0.1.0",
    "iso_url": "https://releases.ubuntu.com/20.04/{{user `template `}}.iso",
    "iso_checksum": "sha256:d1f2bf834bbe9bb43faf16f9be992a6f3935e65be0edece1dee2aa6eb1767423",
    "vm_name": "ubuntu2004",
    "cpus": "4",
    "memory": "2048",
    "disk_size": "65536",
    "hyperv_generation": "2",
    "hyperv_switch": "vagrant",
    "container_runtime": "containerd",
    "ssh_username": "vagrant",
    "ssh_password": "ubuntu",
    "ssh_private_key_file": ".ssh/id_ubuntu",
    "vagrantfile_template": "Vagrantfile.template",
    "boot_command_prefix": "<esc>",
    "build_directory": "build"
  }
}
